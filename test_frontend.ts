import { GoogleGenAI } from '@google/genai';

async function test() {
  try {
    // Wait, I don't have the frontend's injected key.
    // Let's just check if the frontend is actually injecting it.
  } catch (e) {
    console.error('Error:', e);
  }
}

test();
